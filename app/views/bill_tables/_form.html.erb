<%= form_for(@bill_table) do |f| %>
  <% if @bill_table.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bill_table.errors.count, "error") %> prohibited this bill_table from being saved:</h2>

      <ul>
      <% @bill_table.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :comment %><br>
    <%= f.text_field :comment, :value => "client" %><br>
    <%= f.hidden_field :total_price, :value => 0 %><br>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
